/** MAP-004: emisiones reales desde `useMapViewport`; este hook queda reservado para futuras métricas globales. */
export function useMapsRenderAuditStub(): void {
  /* noop — viewport/compositor audit en MapViewport */
}
