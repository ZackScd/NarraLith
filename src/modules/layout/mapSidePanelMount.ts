/** Rail a altura completa (WorkspaceShell, borde derecho). */
export const MAP_SIDE_PANEL_RAIL_MOUNT_ID = "map-side-panel-rail-mount";

/** Panel expandible debajo de la barra superior (MapWorkspace, junto al lienzo). */
export const MAP_SIDE_PANEL_CONTENT_MOUNT_ID = "map-side-panel-content-mount";
